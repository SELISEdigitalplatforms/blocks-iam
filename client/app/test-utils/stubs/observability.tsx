import type { ReactNode } from "react";

export const getRollbar = () => ({});
export const createHttpFailureReporter = () => () => undefined;
export const RollbarProvider = ({ children }: { children: ReactNode }) => children;
